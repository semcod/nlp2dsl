# rest2nlp2dsl

REST API (FastAPI) — endpointy = cienkie wrappery `dsl2nlp2dsl`.

```bash
rest2nlp2dsl serve --port 8212
```

Endpointy: `POST /v1/dsl`, `POST /v1/commands`, `GET /v1/schema/{verb}`, `GET /health`.

Powrót: [packages/README.md](../README.md)
