# mcp2nlp2dsl

Serwer MCP (stdio) — narzędzia = cienkie wrappery `dsl2nlp2dsl`.

```bash
mcp2nlp2dsl serve
```

Narzędzia: `nlp2dsl_run_dsl`, `nlp2dsl_run_command`, `nlp2dsl_to_dsl`.

Powrót: [packages/README.md](../README.md)
