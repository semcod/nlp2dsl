# nlp2nlp2dsl

NL → linia DSL (bez side-effect w `to-dsl`) → opcjonalnie `dispatch()`.

```bash
nlp2nlp2dsl to-dsl "validate workflow.json"
nlp2nlp2dsl apply "plan wyślij fakturę"
nlp2nlp2dsl generate "CRM z kontaktami" --out workflow.json
```

Powrót: [packages/README.md](../README.md)
